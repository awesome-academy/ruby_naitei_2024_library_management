<p><%= t("mailer.unlock.greeting", email: @resource.email) %></p>

<p><%= t("mailer.unlock.account_locked") %></p>

<p><%= t("mailer.unlock.instructions") %></p>

<p><%= link_to t("mailer.unlock.link_text"), unlock_url(@resource, unlock_token: @token) %></p>
